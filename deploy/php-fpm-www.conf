; ISP Platform — FPM pool tuning (see scripts/server-tune.sh).
[www]
user = www-data
group = www-data
listen = 0.0.0.0:9000
pm = ondemand
pm.max_children = 10
pm.process_idle_timeout = 10s
pm.max_requests = 400
clear_env = no
catch_workers_output = yes
decorate_workers_output = no
